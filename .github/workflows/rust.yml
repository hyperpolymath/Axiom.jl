name: Rust
permissions: read-all

on:
  push:
    branches: [ "claude/ferrite-julia-rust-hybrid-01J4hpTCw5mmRbb73fmWrM5Q" ]
  pull_request:
    branches: [ "claude/ferrite-julia-rust-hybrid-01J4hpTCw5mmRbb73fmWrM5Q" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose
